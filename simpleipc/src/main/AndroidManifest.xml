<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.com.hwtc.simpleipc"
    >

  <uses-permission android:name="android.permission.BROADCAST_STICKY"/>

  <uses-permission android:name="cn.com.hwtc.permission.IPC" />

  <!-- IPC  Permission -->
  <permission
      android:name="cn.com.hwtc.permission.IPC"
      android:label="@string/ipc_permission_description"
      android:permissionGroup="@string/ipc_permission_group"
      android:protectionLevel="signature" >
  </permission>

  <application
      android:allowBackup="false"
      android:label="@string/app_name"
      android:supportsRtl="true"
      >

  </application>

</manifest>
